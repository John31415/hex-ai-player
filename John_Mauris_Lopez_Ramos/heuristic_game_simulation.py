from board import HexBoard
import random

class HeuristicGameSimulation:

    def __init__(self, board: HexBoard, player_id):
        pass

    # returns true if player_id wins the simulation
    def simulation(self) -> bool:
        return int(random.random() * 2)